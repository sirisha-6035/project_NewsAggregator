extends layout 

block content-head 
  title= title 

block content-body   
  section.error-section
    .error-container
      h2 #{statusCode || 'Error'}! Error
      p.error-message #{message || 'Something went wrong.'}
      // Use a relative path to the homepage
      a.error-link(href="/") Return to home
